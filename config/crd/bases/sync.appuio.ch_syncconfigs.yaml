
---
apiVersion: apiextensions.k8s.io/v1beta1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.3.0
  creationTimestamp: null
  name: syncconfigs.sync.appuio.ch
spec:
  group: sync.appuio.ch
  names:
    kind: SyncConfig
    listKind: SyncConfigList
    plural: syncconfigs
    singular: syncconfig
  scope: Namespaced
  subresources:
    status: {}
  validation:
    openAPIV3Schema:
      description: SyncConfig is the Schema for the syncconfigs API
      properties:
        apiVersion:
          description: 'APIVersion defines the versioned schema of this representation
            of an object. Servers should convert recognized schemas to the latest
            internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
          type: string
        kind:
          description: 'Kind is a string value representing the REST resource this
            object represents. Servers may infer this from the endpoint the client
            submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
          type: string
        metadata:
          type: object
        spec:
          properties:
            deleteItems:
              description: DeleteItems lists items to be deleted from targeted namespaces
              items:
                description: DeleteMeta defines an object by name, kind and version
                properties:
                  apiVersion:
                    description: APIVersion of the item to be deleted
                    type: string
                  kind:
                    description: Kind of the item to be deleted
                    type: string
                  name:
                    description: Name of the item to be deleted
                    type: string
                type: object
              type: array
            forceRecreate:
              description: ForceRecreate defines if objects should be deleted and
                recreated if updates fails
              type: boolean
            namespaceSelector:
              description: NamespaceSelector defines which namespaces should be targeted
              properties:
                ignoreNames:
                  description: IgnoreNames lists namespace names to be ignored. Each
                    entry can be a Regex pattern and if they match the namespaces
                    will be excluded from the sync even if matching in "matchNames"
                    or via LabelSelector. A namespace is ignored if at least one pattern
                    matches. Invalid patterns will cause the sync to be cancelled
                    and the status conditions will contain the error message.
                  items:
                    type: string
                  type: array
                labelSelector:
                  description: LabelSelector of namespaces to be targeted. Can be
                    combined with MatchNames to include unlabelled namespaces.
                  properties:
                    matchExpressions:
                      description: matchExpressions is a list of label selector requirements.
                        The requirements are ANDed.
                      items:
                        description: A label selector requirement is a selector that
                          contains values, a key, and an operator that relates the
                          key and values.
                        properties:
                          key:
                            description: key is the label key that the selector applies
                              to.
                            type: string
                          operator:
                            description: operator represents a key's relationship
                              to a set of values. Valid operators are In, NotIn, Exists
                              and DoesNotExist.
                            type: string
                          values:
                            description: values is an array of string values. If the
                              operator is In or NotIn, the values array must be non-empty.
                              If the operator is Exists or DoesNotExist, the values
                              array must be empty. This array is replaced during a
                              strategic merge patch.
                            items:
                              type: string
                            type: array
                        required:
                        - key
                        - operator
                        type: object
                      type: array
                    matchLabels:
                      additionalProperties:
                        type: string
                      description: matchLabels is a map of {key,value} pairs. A single
                        {key,value} in the matchLabels map is equivalent to an element
                        of matchExpressions, whose key field is "key", the operator
                        is "In", and the values array contains only "value". The requirements
                        are ANDed.
                      type: object
                  type: object
                matchNames:
                  description: MatchNames lists namespace names to be targeted. Each
                    entry can be a Regex pattern. A namespace is included if at least
                    one pattern matches. Invalid patterns will cause the sync to be
                    cancelled and the status conditions will contain the error message.
                  items:
                    type: string
                  type: array
              type: object
            syncItems:
              description: SyncItems lists items to be synced to targeted namespaces
              items:
                type: object
              type: array
          type: object
        status:
          description: SyncConfigStatus defines the observed state of SyncConfig
          properties:
            conditions:
              description: Conditions contain the states of the SyncConfig. A SyncConfig
                is considered Ready when at least one item has been synced.
              items:
                description: SyncConfigCondition describes a status condition of a
                  SyncConfig
                properties:
                  lastTransitionTime:
                    format: date-time
                    type: string
                  message:
                    type: string
                  reason:
                    type: string
                  status:
                    type: string
                  type:
                    description: SyncConfigConditionType identifies the type of a
                      condition. The type is unique in the Status field.
                    type: string
                required:
                - status
                - type
                type: object
              type: array
            deletedItemCount:
              description: DeletedItemCount holds the accumulated number of deleted
                objects from targeted namespaces. Inexisting items do not get counted.
              format: int64
              type: integer
            failedItemCount:
              description: FailedItemCount holds the accumulated number of objects
                that could not be created, updated or deleted. Inexisting items do
                not get counted.
              format: int64
              type: integer
            synchronizedItemCount:
              description: SynchronizedItemCount holds the accumulated number of created
                or updated objects in the targeted namespaces.
              format: int64
              type: integer
          required:
          - deletedItemCount
          - failedItemCount
          - synchronizedItemCount
          type: object
      type: object
  version: v1alpha1
  versions:
  - name: v1alpha1
    served: true
    storage: true
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []
